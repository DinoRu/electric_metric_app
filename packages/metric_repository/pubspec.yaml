name: metric_repository
description: "Handling metrics package."
version: 0.0.1
homepage:

environment:
  sdk: ">=3.4.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  http: any
  flutter:
    sdk: flutter
  bloc:
  flutter_bloc:
  equatable:
  sqflite: ^2.3.3+1
  path_provider: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  user_repository:
    path: ../user_repository

flutter:
